pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

rootProject.name = 'podo-dev'

include 'podo-dev-core',
        'podo-dev-rest-client',
        'podo-dev-web',
        'podo-dev-uploader'
