#Server Config
server:
  port: 38443
  http:
    port: 38080

spring:
  profiles:
#    include: secret, core
    include: secret, core,deploy

  # Servlet
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB


uploader:
  security:
    client:
      tokens: mciGpOzzSvncQJ5IFEUNVODRyVkgL1kD
  frontend:
    subpath: /uploaded
  upload:
    base:
    #      dir: /upload
      dir: C://podo-dev-uploader/upload
---

spring:
  profiles: secret, deploy
  jpa:
    show-sql: false

logging:
  config: classpath:logback-daily.xml

uploader:
  upload:
    base:
      dir: /uploaded



